/* ---------- header ---------- */
"use strict";
/* ---------- header ---------- */

var csaSpecialMoveMap = {
  '%TORYO'        : '%TORYO',
  '%TSUMI'        : '%TSUMI',
  '%SENNICHITE'   : '%SENNICHITE',
  '%CHUDAN'       : '%CHUDAN',
  '%JISHOGI'      : '%JISHOGI',
  '%ILLEGAL_MOVE' : '%ILLEGAL_MOVE'
};

/* ---------- exports ---------- */
module.exports = csaSpecialMoveMap;
/* ---------- exports ---------- */
